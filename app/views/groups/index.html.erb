<p style="color: green"><%= notice %></p>

<div id="groups">
  <% @groups.each do |group| %>
    <%= link_to group_investments_path(group), method: :get, class: "box" do %>
      <%= render group %>
    <% end %>
  <% end %>
</div>
<br>
<div>
  <%= button_to "New group", new_group_path, {method: :get, class: "button is-success is-large is-fullwidth"} %>
</div>
